services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: foundry-postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    restart: unless-stopped
    environment:
      - POSTGRES_USER=foundry
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-foundry}
      - POSTGRES_DB=foundry_bridge
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U foundry -d foundry_bridge"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  migrate:
    build:
      context: .
      target: migrate
    container_name: foundry-migrate
    env_file:
      - .env
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgresql://foundry:${POSTGRES_PASSWORD:-foundry}@postgres:5432/foundry_bridge
    command: ["/app/.venv/bin/alembic", "upgrade", "head"]

  bridge:
    build:
      context: .
      target: bridge
    container_name: foundry-bridge
    env_file:
      - .env
    ports:
      - "8765:8765"
      - "8766:8766"
      - "8767:8767"
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    environment:
      - PYTHONUNBUFFERED=1
      - AGENT_DEBUG=${AGENT_DEBUG:-true}
      - DATABASE_URL=postgresql://foundry:${POSTGRES_PASSWORD:-foundry}@postgres:5432/foundry_bridge
      - MODEL_PROVIDER=${MODEL_PROVIDER:-openai}
      - MODEL=${MODEL:-gpt-5.4}
      - NOTE_CADENCE_MINUTES=${NOTE_CADENCE_MINUTES:-1}
      - WS_PORT=${WS_PORT:-8765}
      - HTTP_PORT=${HTTP_PORT:-8766}
      - API_PORT=${API_PORT:-8767}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_COLOR=${LOG_COLOR:-true}
      - FASTEMBED_CACHE_PATH=/app/.cache/fastembed
      - RECENT_NOTES_LIMIT=${RECENT_NOTES_LIMIT:-3}
    volumes:
      - fastembed_cache:/app/.cache/fastembed
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8766/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

volumes:
  postgres_data:
  fastembed_cache:
