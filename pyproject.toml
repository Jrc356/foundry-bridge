[project]
name = "foundry-bridge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=16.0",
]

[project.optional-dependencies]
transcriber = [
    "deepgram-sdk>=3.0",
]
